//functions that have not been grouped into a library yet.

var m = {};
m.cmd = require('./cmd');

m.createEvent = function (event) {
  //todo figure out what an event is.
  console.log(event, "event was created!")

};

module.exports = m;